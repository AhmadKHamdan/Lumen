# Lumen exploration prototype (webdemo) — runtime dependencies.
fastapi>=0.110
uvicorn>=0.29
opencv-python>=4.8
numpy>=1.24
ultralytics>=8.1        # YOLOv8m (COCO indicators) + the custom door models
pillow>=10.0            # frame handoff to the depth model

# Optional — enables the Phase-B depth obstacle layer (Depth Anything V2 Small).
# Without these the obstacle watchdog runs YOLO-only (named objects); everything
# else is unaffected.
torch>=2.0
transformers>=4.40
