# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# Environment
.env
.env.local

# Whisper model cache (downloaded on first run, ~140MB)
~/.cache/whisper/

# Captured PTT audio dumps from dev/debugging
backend/captured_audio/*
!backend/captured_audio/.gitkeep

# ML model weights (auto-downloaded on first inference)
*.pt
*.task
*.pth
*.onnx
*.engine
backend/yolov8n.pt
backend/hand_landmarker.task

# Frontend
node_modules/
dist/
build/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
logs/

# Local TLS certs (when we add HTTPS in Sprint 2)
*.pem
*.key
*.crt

# ---- Navigation exploration prototype (webdemo) ----
# Door-training datasets (large)
door_training/doors_dataset/
door_training/doors_dataset_4cls/
door_training/doors_dataset.zip
door_training/converted/
*.zip
# Training / inference output
runs/
door_training/val_out.txt

# ---- Trained models the demo REQUIRES (exceptions to *.pt / runs/ above, so a
#      fresh clone is self-sufficient and the demo runs out of the box) ----
# The single-class door detector at the repo root:
!/best.pt
# The 4-class door verifier lives under an ignored runs/ tree, so we must re-include
# each directory on its path before we can re-include the file itself (git won't
# look inside an excluded directory). Re-include the dir, re-exclude its contents,
# then drill down — so ONLY this one weight file comes back, not the whole run.
!door_training/runs/
door_training/runs/*
!door_training/runs/detect/
door_training/runs/detect/*
!door_training/runs/detect/door_yolov8s_4cls/
door_training/runs/detect/door_yolov8s_4cls/*
!door_training/runs/detect/door_yolov8s_4cls/weights/
door_training/runs/detect/door_yolov8s_4cls/weights/*
!door_training/runs/detect/door_yolov8s_4cls/weights/best.pt

# ---- LaTeX report build artifacts ----
report/*.aux
report/*.log
report/*.out
report/*.toc
report/*.lof
report/*.lot
report/*.bbl
report/*.bcf
report/*.blg
report/*.run.xml
report/preview-sandbox.pdf
report/*.fdb_latexmk
report/*.fls
report/*.tmp

# Scratch TTS output at the repo root
/output.mp3
